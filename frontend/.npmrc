# react-scripts 5.x tiene vulnerabilidades conocidas en dependencias de DEV
# (webpack, postcss, etc.) que NO afectan al build de producción.
# NO ejecutar "npm audit fix --force" — destruye react-scripts.
audit=false
legacy-peer-deps=true
